export default function UploadTab() {
  return (
    <section className="upload-tab">
      <div className="placeholder">
        Document Upload UI lands in Stage 4. The backend endpoint is already live at
        <code> POST /api/upload</code>.
      </div>
    </section>
  )
}
